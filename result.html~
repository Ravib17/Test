
	 <div id="background" >
	  <div id="shopdetails">
			<table border=1>
				<caption>Shop Details</caption>
				<tr><th>Shop Name</th><th>Owner Name</th><th>Type</th><th>Area</th><th>City</th><th>District</th><th>Pincode</th><th>Budget</th><th>
				Delete</th></tr>
				<tr ng-repeat="shop in shops">
					<td>{{shop.shop_name | capitalizeFirstLetter}}</td>
					 <td>{{shop.owner_name | capitalizeFirstLetter}}</td>
					 <td>{{shop.type | capitalizeFirstLetter}}</td> 
					  <td>{{shop.area | capitalizeFirstLetter}} </td>
					 <td>{{shop.city | capitalizeFirstLetter}} </td> 
					 <td>{{shop.district}}</td> 
					 <td>{{shop.pincode}}</td>
					 <td>{{shop.budget}}</td>
					 <td><button style="background-color:red;color:white;border-radius:50%;cursor:pointer" ng-click="deleteShop(shop)" title="Delete Shop Details"/></a>X</button></td>
					 
				</tr>
				<tr><td colspan=9><a ui-sref='addshop'  id="btn-addAnother"><button style="background-color: #7B3D11;color:white;cursor:pointer" title="Add shop ">Add</button></a></td></tr>
			</table>
		</div>
		 <div id="result">
		   
		  <table border=1>
			<caption>Result</caption
			<tr><th>Shop Type</th><th>Shops</th><th>Average Budget</th></tr>
			<tr ng-repeat="x in result">
				<td>{{ x.type | capitalizeFirstLetter }}</td>
				<td>{{ x.totalshops }}</td>
				<td>{{ x.average }}</td>
			</tr>
			<tr>
				<td>Total</td>
				<td>{{ totalShops }}</td>
				<td>{{ totalAverage }}</td>
			</tr>
			
		  </table>
		  
		  
		  </div>
	 </div>
	
